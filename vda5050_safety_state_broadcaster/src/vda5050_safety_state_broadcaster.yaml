vda5050_safety_state_broadcaster:
  interfaces:
    field_violation:
      type: string_array
      default_value: []
      description: "Interface names for field violation events. Supports double (1.0) or bool (true)."

    e_stop:
      manual:
        type: string_array
        default_value: []
        description: "Interface names for manual e-stop events. Supports double (1.0) or bool (true)."
      remote:
        type: string_array
        default_value: []
        description: "Interface names for remote e-stop events. Supports double (1.0) or bool (true)."
      autoack:
        type: string_array
        default_value: []
        description: "Interface names for autoack e-stop events. Supports double (1.0) or bool (true)."
