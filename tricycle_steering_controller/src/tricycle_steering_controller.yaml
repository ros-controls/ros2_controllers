tricycle_steering_controller:
  traction_track_width:
    {
      type: double,
      default_value: 0.0,
      description: "Axle track, only for dual-traction configuration. Distance between the two traction wheels.",
      read_only: false,
    }

  wheelbase:
    {
      type: double,
      default_value: 0.0,
      description: "Distance between front and rear wheels. For details see: https://en.wikipedia.org/wiki/Wheelbase",
      read_only: false,
      validation: {
        gt<>: [0.0]
      }
    }

  traction_wheels_radius:
    {
      type: double,
      default_value: 0.0,
      description: "Traction wheels radius.",
      read_only: false,
      validation: {
        gt<>: [0.0]
      }
    }
