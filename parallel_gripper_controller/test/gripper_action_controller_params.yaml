test_gripper_action_position_controller:
  ros__parameters:
    joint: "joint1"

test_gripper_action_position_controller_empty_joint:
  ros__parameters:
    joint: ""

test_gripper_controller_with_effort:
  ros__parameters:
    joint: "joint1"
    max_effort_interface: "joint1/effort"

test_gripper_controller_with_velocity:
  ros__parameters:
    joint: "joint1"
    max_velocity_interface: "joint1/velocity"

test_gripper_controller_with_effort_and_velocity:
  ros__parameters:
    joint: "joint1"
    max_effort_interface: "joint1/effort"
    max_velocity_interface: "joint1/velocity"
