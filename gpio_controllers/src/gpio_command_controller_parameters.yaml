gpio_command_controller_parameters:
  gpios: {
    type: string_array,
    description: "List of gpios",
    validation: {
      size_gt<>: [0],
      unique<>: null
    }
  }

  command_interfaces:
    __map_gpios:
      interfaces: {
        type: string_array,
        description: "List of interfaces for each gpio",
        validation: {
          size_gt<>: [0],
          unique<>: null
        }
      }
