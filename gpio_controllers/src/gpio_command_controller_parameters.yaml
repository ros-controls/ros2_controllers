gpio_command_controller_parameters:
  gpios: {
    type: string_array,
    description: "List of gpios",
    validation: {
      size_gt<>: [0],
      unique<>: null
    }
  }

  command_interfaces:
    __map_gpios:
      ports: {
        type: string_array,
        description: "List of ports: for each gpio",
        validation: {
          size_gt<>: [0],
          unique<>: null
        }
      }
